{
  "name": "linkedin-cli-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "dev": "concurrently -n api,web -c yellow,blue \"uv run --package linkedin-api python apps/api/manage.py runserver 127.0.0.1:8000\" \"pnpm --filter web dev\"",
    "dev:api": "uv run --package linkedin-api python apps/api/manage.py runserver 127.0.0.1:8000",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "test": "uv run --package linkedin-api python apps/api/manage.py test login_api && uv run --package linkedin-cli pytest apps/cli/tests/unit",
    "cli": "uv run --package linkedin-cli linkedin-cli"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
